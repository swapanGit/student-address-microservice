server:
  port: 8787

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/swapanGit/cloud-config
          clone-on-start: true